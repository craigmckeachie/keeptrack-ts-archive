#!/bin/bash
  cd ~/Documents/git/react/keeptrack-ts

  git checkout lab01 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab02 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab03 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab04 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab05 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab06 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab07 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab08 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab09 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab10 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab11 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab12 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab13 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab14 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab15 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab16 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab17 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab18 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab19 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab20 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab21 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab22 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab23 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab24 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab25 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab26 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab27 -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab21a -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab21b -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout lab21c -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout redux-toolkit -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout redux-toolkit-query -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout labauth -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
    
  git checkout testing -f
  npm install
  read -t 3 -p "I am going to wait for 3 seconds only ..."
  npm start
  # Ctrl + C
  npm test
  # Ctrl + C
    